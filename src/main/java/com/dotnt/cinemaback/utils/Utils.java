package com.dotnt.cinemaback.utils;

public class Utils {
//    public BigDecimal calculateTotalSeatPrice(List<Seat> seats) {
//        return seats.stream()
//                .map(seat -> BigDecimal.valueOf(seat.getSeatType().getPrice()))
//                .reduce(BigDecimal.ZERO, BigDecimal::add);
//    }
}
